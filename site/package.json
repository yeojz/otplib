{
  "name": "otplib-site",
  "version": "1.0.0",
  "description": "Website for otplib",
  "homepage": "https://yeojz.github.io/otplib",
  "main": "build.js",
  "scripts": {
    "build": "npm run build:public",
    "build:all": "cross-env BUILD_PKG=index,copy,docs node ./build.js",
    "build:index": "cross-env BUILD_PKG=index node ./build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "otplib"
  ],
  "author": "Gerald Yeo",
  "license": "MIT",
  "dependencies": {
    "bulma": "^0.4.0",
    "copy": "^0.3.0",
    "cross-env": "^4.0.0",
    "ncp": "^2.0.0"
  }
}
