{
  "name": "otplib-site",
  "version": "1.0.0",
  "description": "Website for otplib",
  "homepage": "https://yeojz.github.io/otplib",
  "main": "build.js",
  "scripts": {
    "build": "PUBLIC_URL= ./scripts/build.sh",
    "build:deps": "./scripts/build-deps.sh",
    "build:dev": "PUBLIC_URL=/ ./scripts/build.sh",
    "clean": "rimraf dist",
    "deploy": "gh-pages -t -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "otplib"
  ],
  "author": "Gerald Yeo <contact@fusedthought.com>",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^2.0.0",
    "highlight.js": "^9.10.0",
    "qrcode": "^1.3.3",
    "rimraf": "^3.0.0"
  }
}
