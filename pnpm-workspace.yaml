packages:
  - "apps/*"
  - "internal/*"
  - "packages/*"
  # Separately locked Stryker toolchain — see internal/mutation/README.md
  - "!internal/mutation"

# Delay installing newly published versions to reduce risk of compromised packages
minimumReleaseAge: 10080
