pre-commit:
  parallel: true
  commands:
    format:
      run: pnpm format:check
      stage_fixed: true
    lint:
      run: pnpm lint
    typecheck:
      run: pnpm typecheck

pre-push:
  parallel: true
  commands:
    test:
      run: pnpm test
