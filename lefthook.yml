pre-commit:
  parallel: true
  commands:
    format:
      run: npm run format:check
      stage_fixed: true
    lint:
      run: npm run lint
    typecheck:
      run: npm run typecheck

pre-push:
  parallel: true
  commands:
    test:
      run: npm test
